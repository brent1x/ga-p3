— Add Restaurant PAGE —

<%= simple_form_for @restaurant, :url => add_restaurant_path do |f| %>
  <%= f.input :name %>
  <%= f.input :city %>
  <%= f.input :state %>
  <%= f.button :submit %>
<% end %>
